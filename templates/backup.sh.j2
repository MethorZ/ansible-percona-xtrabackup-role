#!/bin/sh
##
# Ansible managed file - DO NOT EDIT!
##
currentDate=$(date +'%Y-%m-%d-%H-%M-%S')
xtrabackup --backup --user={{ percona_xtrabackup_user }} --target-dir={{ percona_xtrabackup_dir }}/"${currentDate}"
LATEST_BACKUP=$(ls -td {{ percona_xtrabackup_dir }}/20*/ | head -1)
xtrabackup --prepare --target-dir=$LATEST_BACKUP
